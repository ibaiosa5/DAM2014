// APP.FizzBuzz = (function(){})() igual a
define('FizzBuzz',['Fizz','Buzz'],function(){
    'use strict';

    var validators=[];

    var testNumber = function(number){
        var n = num||1;
        red=[];

        for(var i=0;i<validators.legth;i++){
            if(validators[i].comprobar(n)){
                res.push
            }
        }
    };

    var print = function(num, join){

    }

    var addValidator = function(validator){
        if(validator.hasOwnProperty('comprobar'))
    }

    for(var i=0;i<arguments.length;i++){//arguments recibe los argumentos que le pasa a la funcion en este caso Fizz y Buzz

    }

    return {
        testNumber:testNumber
    };
});